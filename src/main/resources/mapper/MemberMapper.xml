<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.MemberMapper">

    <select id="countByMbrId" resultType="int">
        SELECT COUNT(*)
          FROM MEMBER
         WHERE MBR_ID = #{mbrId}
           AND DEL_YN = 'N'
    </select>

    <insert id="insertMember" parameterType="Member">
        <selectKey keyProperty="mbrNo" resultType="long" order="BEFORE">
            SELECT SEQ_MBR_NO.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO MEMBER (
            MBR_NO
          , MBR_ID
          , MBR_PWD
          , MBR_NM
          , EMAIL
          , MOBILE_NO
          , REG_ID
        ) VALUES (
            #{mbrNo}
          , #{mbrId}
          , #{mbrPwd}
          , #{mbrNm}
          , #{email}
          , #{mobileNo}
          , #{mbrId}
        )
    </insert>

</mapper>